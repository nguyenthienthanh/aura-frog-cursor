{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/settings-schema.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$TOOL_INPUT\" == *\"git commit\"* ]]; then echo '[REMINDER] Update CHANGELOG.md and increment version if needed.'; fi"
          }
        ]
      }
    ]
  }
}
