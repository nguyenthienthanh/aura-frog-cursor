# Examples - aura-frog-cursor

Auto-generated by project:regen on 2026-02-13

## Project Structure Example

```
aura-frog-cursor/
├── .claude/
│   ├── CLAUDE.md                # Claude Code integration
│   ├── cache/                   # Memory cache
│   ├── project-contexts/        # Project context (this!)
│   └── settings.json            # Claude settings
├── .cursor/
│   ├── mcp.json                 # 13 MCP servers
│   ├── commands/                # 87 slash commands
│   ├── rules/                   # 90+ rules total
│   ├── workflows/               # 9-phase TDD
│   ├── docs/                    # Documentation
│   └── templates/               # Document templates
├── scripts/
│   ├── sync-version.sh          # Version sync
│   ├── learn/                   # Learning scripts
│   └── visual/                  # Visual regression scripts
├── setup.sh                     # Installation script
├── VERSION                      # Single version source
├── CHANGELOG.md                 # Change history
└── README.md                    # Project documentation
```

## MCP Server Config Example

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"]
    },
    "firebase": {
      "command": "npx",
      "args": ["-y", "firebase-tools@latest", "mcp"],
      "disabled": true
    }
  }
}
```

## Command Structure Example

Commands follow the pattern: `category/action.md`

```markdown
# Command: category:action

**Category:** CategoryName
**Priority:** High|Medium|Low
**Syntax:** `category:action [args]`

## Description
What this command does...

## Execution Steps
1. Step one
2. Step two

## Output
Expected output format...
```

## Agent Rule Example (.mdc format)

Agent rules use Cursor's `.mdc` format with frontmatter:

```
---
description: Agent description for auto-detection
globs:
alwaysApply: false
---

# Agent: agent-name

## Expertise
- Skill 1
- Skill 2

## When to Activate
- Trigger condition 1
- Trigger condition 2
```

## Workflow Phase Example

```
Phase 1: Understand → Requirements analysis
Phase 2: Design → Technical planning
Phase 3: UI Breakdown → Component structure
Phase 4: Test Planning → Test strategy
Phase 5: TDD → RED (5a) → GREEN (5b) → REFACTOR (5c)
Phase 6: Review → Code quality check
Phase 7: Verify → QA validation
Phase 8: Document → Documentation
Phase 9: Share → Deployment
```

## Version Sync Example

```bash
# Bump patch version and sync
./scripts/sync-version.sh --bump patch --with-date

# Preview changes
./scripts/sync-version.sh --dry-run
```
