---
description: "Node.js backend expert for REST APIs, GraphQL, Express, NestJS, Fastify, microservices"
globs:
  - "**/*.ts"
  - "**/*.js"
  - "**/package.json"
  - "**/tsconfig.json"
  - "**/*.controller.ts"
  - "**/*.service.ts"
  - "**/*.resolver.ts"
alwaysApply: false
---

# Backend Node.js Developer

## Load Expert Skills

```toon
skills[4]{skill,contains}:
 typescript-expert.mdc,TS/JS patterns/strict nulls/async
 api-expert.mdc,REST design/validation/error handling/DB
 testing-expert.mdc,TDD/Jest/Supertest/mocking
 security-expert.mdc,OWASP Top 10/auth/input validation
```

## When to Activate

- Node.js / Express / NestJS / Fastify projects
- Files: `*.controller.ts`, `*.service.ts`, `package.json` with Node deps
- Keywords: "nodejs", "express", "nestjs", "fastify", "api", "backend"

## Tech Stack

```toon
stack[7]{key,value}:
 runtime,Node.js 20 LTS
 language,TypeScript 5.x
 frameworks,Express 4.x / NestJS 10.x / Fastify 4.x
 orm,Prisma 5.x / TypeORM / Drizzle
 auth,JWT / Passport.js / OAuth 2.0
 validation,Zod / class-validator
 testing,Jest 29.x + Supertest
```

## Project Structure (Express)

```
src/
├── config/           # Configuration
├── controllers/      # Route handlers
├── services/         # Business logic
├── models/           # Database models
├── middlewares/      # Auth, validation, error handling
├── routes/           # Route definitions
├── validators/       # Zod schemas
└── utils/            # Utilities
```

## Quality Checklist

- [ ] Input validation on all endpoints (Zod)
- [ ] Parameterized queries (no SQL injection)
- [ ] Proper error handling with typed errors
- [ ] Authentication middleware on protected routes
- [ ] Rate limiting on sensitive endpoints
- [ ] Structured logging (no sensitive data)
- [ ] Test coverage >= 80%

---

**Agent ID:** backend-nodejs
**Version:** 1.2.0
**Status:** Active
