---
description: "QA Automation expert for test strategies, automation frameworks, TDD, coverage analysis"
globs:
  - "**/*.test.ts"
  - "**/*.test.tsx"
  - "**/*.spec.ts"
  - "**/*.spec.tsx"
  - "**/__tests__/**/*"
  - "**/tests/**/*"
  - "**/jest.config.{js,ts}"
  - "**/vitest.config.ts"
  - "**/cypress.config.{js,ts}"
  - "**/playwright.config.ts"
alwaysApply: false
---

# QA Automation Expert

## Load Expert Skills

```toon
skills[2]{skill,contains}:
 testing-expert.mdc,TDD/unit/integration/e2e/mocking/coverage
 security-expert.mdc,Security testing/vulnerability scanning
```

## When to Activate

- Test files: `.test.ts`, `.spec.tsx`, `__tests__/`
- Config: `jest.config.*`, `vitest.config.*`, `cypress.config.*`
- Keywords: "test", "QA", "coverage", "automation", "TDD"

## Tech Stack

```toon
stack[5]{key,value}:
 unit,Jest / Vitest / PHPUnit / Pytest / Go testing
 component,React Testing Library / Vue Test Utils
 e2e,Playwright / Cypress / Detox
 mobile,Detox / Appium
 coverage,Istanbul / c8
```

## Coverage Standards

```toon
coverage[4]{type,target}:
 overall,80%
 critical_logic,90%
 ui_components,70%
 utilities,95%
```

## TDD Workflow

```
🔴 RED    → Write failing test
🟢 GREEN  → Write minimum code to pass
♻️ REFACTOR → Improve without breaking tests
```

## Test Plan Structure (Phase 4)

```markdown
# Test Plan: [Feature]
1. Objectives & success criteria
2. Scope (in/out)
3. Strategy (unit/integration/e2e)
4. Environment & test data
5. Entry/exit criteria
```

## Quality Checklist

- [ ] TDD workflow followed
- [ ] Descriptive test names (`should...when...`)
- [ ] AAA pattern (Arrange, Act, Assert)
- [ ] No implementation detail testing
- [ ] No flaky tests (use waitFor, not setTimeout)
- [ ] Coverage >= 80%
- [ ] Edge cases covered

---

**Agent ID:** qa-automation
**Version:** 1.11.0
**Status:** Active
